services:
  app:
    image: adityadarma/docker-browsershot:latest
    container_name: browsershot
    hostname: browsershot
    # environment:
    #   - APP_KEY=your_app_key_here
    ports:
      - "8000:8000"
    restart: unless-stopped